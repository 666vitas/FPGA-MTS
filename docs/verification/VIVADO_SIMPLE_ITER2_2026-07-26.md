Status: ACTIVE
Effective-Gate: LOCK-MVP-L1
Authority: VERIFICATION
Last-Updated: 2026-07-26

# Vivado SIMPLE Iteration 2 Verification Summary

## 1. Input reports

The user supplied the following routed-design reports generated by Vivado 2020.1 for `red_pitaya_top` on `xc7z010clg400-1`:

- `01_timing_summary.rpt`
- `02_worst_setup_paths.rpt`
- `03_high_fanout.rpt`
- `04_drc.rpt`
- `05_methodology.rpt`
- `06_utilization.rpt`

Report time: 2026-07-26 12:47.

## 2. Timing result

The routed implementation meets every currently specified timing constraint:

```text
WNS  = +0.209 ns
TNS  =  0.000 ns
Setup failing endpoints = 0 / 22806
WHS  = +0.050 ns
THS  =  0.000 ns
Hold failing endpoints  = 0 / 22806
WPWS = +1.000 ns
TPWS =  0.000 ns
Pulse-width failing endpoints = 0 / 8429
Failed routes = 0
```

Vivado states: `All user specified timing constraints are met.`

This closes the previous routed setup failure in `i_ramp_generator`.

## 3. Timing coverage is not yet sign-off clean

`check_timing` still reports:

```text
no_clock                         = 52
unconstrained_internal_endpoints = 19
no_input_delay                   = 17
no_output_delay                  = 42
multiple_clock                   = 0
generated_clocks                 = 0
loops                            = 0
```

Therefore this result is a **timing-clean candidate for the constrained paths**, but it is not yet a full timing sign-off. The 19 unconstrained internal endpoints and 52 no-clock pins must be identified and classified before a production bitstream is approved.

No blanket `false_path`, `multicycle`, clock relaxation, or frequency reduction may be added merely to hide these reports. Each exception must be tied to a documented asynchronous, static, test-only, or unused path.

## 4. Worst setup path

The worst setup path is now inside the custom OUT2 controller:

```text
Source      : i_out2_lock_controller/s5_lock_limit_reg[12]/C
Destination : i_out2_lock_controller/control_o_reg[5]/D
Clock       : pll_adc_clk, 125 MHz, period 8.000 ns
Slack       : +0.209 ns
Data delay  : 7.697 ns
Logic delay : 4.341 ns
Route delay : 3.356 ns
Logic levels: 16, including 12 CARRY4 cells
```

Decision:

- do not add new combinational lock-acquisition decisions into the `control_o` output stage;
- keep realtime ERROR-crossing detection in the acquisition block;
- any future slew limiter, supervisor, or P soft-start must be pipelined and re-run through implementation;
- the current +0.209 ns margin is usable but small.

## 5. Methodology critical warnings

`report_methodology` contains 5 critical warnings:

```text
TIMING-6  No common primary clock between par_clk and pll_adc_clk : 2
TIMING-7  No common node between par_clk and pll_adc_clk          : 2
TIMING-17 i_hk/i_DNA/CLK is not reached by a timing clock         : 1
```

Required follow-up:

1. Generate explicit timing reports in both directions between `par_clk` and `pll_adc_clk`.
2. Determine whether those crossings use an intentional CDC structure, are functionally unused in this build, or require corrected clock constraints.
3. Identify the 19 unconstrained endpoints by object name.
4. Classify `i_hk/i_DNA/CLK`; do not suppress it until the housekeeping DNA path is understood.

Recommended Vivado commands:

```tcl
open_run impl_1

report_timing -from [get_clocks par_clk] -to [get_clocks pll_adc_clk] \
  -max_paths 50 -file ../timing_for_codex/simple_iter2/07_par_to_adc.rpt

report_timing -from [get_clocks pll_adc_clk] -to [get_clocks par_clk] \
  -max_paths 50 -file ../timing_for_codex/simple_iter2/08_adc_to_par.rpt

report_clock_interaction \
  -file ../timing_for_codex/simple_iter2/09_clock_interaction.rpt

report_timing -unconstrained -max_paths 200 \
  -file ../timing_for_codex/simple_iter2/10_unconstrained_paths.rpt
```

## 6. DRC and non-critical methodology findings

`report_drc` contains 45 warning/advisory violations and no error-level rule in the summary:

```text
DPIP-1   input DSP pipelining      : 3
DPOP-1   DSP PREG pipelining       : 5
DPOP-2   DSP MREG pipelining       : 7
PDRC-153 gated clock check         : 1
PLIO-8   IO placement constraints  : 28
REQP-24  advisory                  : 1
```

One custom-path warning is relevant:

```text
i_laser_lock_core/i_mixer_core/product_w has DSP PREG=0
```

The mixer path currently meets timing and must not be reworked during the realtime-crossing gate unless a new implementation report proves it is necessary. The warning is recorded for a later error-signal bandwidth/noise gate.

## 7. High fanout

The top high-fanout nets are dominated by the inherited Red Pitaya ASG, reset, scope, and PS/AXI logic. The custom SIMPLE acquisition request/snapshot nets are not in the top-20 report.

Largest entries include:

```text
i_asg/ch[0]/SR[0] : 3498
rstn               : 305
```

This confirms that the prior acquisition-specific fanout problem is no longer the current timing limiter.

## 8. Utilization

Routed top-level utilization:

```text
Total LUTs : 6819
Logic LUTs : 6456
LUTRAMs    : 160
SRLs       : 203
FFs        : 7688
RAMB36     : 38
DSP blocks : 12
```

Custom hierarchy:

```text
custom_register_bank       : 544 LUT, 652 FF
simple_lock_acquisition    : 262 LUT, 117 FF
custom_debug_capture       :  99 LUT,  91 FF, 8 RAMB36
laser_lock_core            : 200 LUT,  74 FF, 1 DSP
out2_lock_controller       : 313 LUT, 189 FF, 1 DSP
ramp_generator             : 156 LUT, 216 FF
```

There is enough device capacity for a small realtime crossing detector. The main risk is timing margin, not device utilization.

## 9. Verification decision

### Passed

- routed setup timing on all constrained paths;
- routed hold timing on all constrained paths;
- zero setup/hold/pulse-width failing endpoints;
- zero failed routes;
- previous ramp timing blocker removed.

### Not passed

- full timing coverage;
- 19 unconstrained internal endpoints;
- 52 no-clock pins;
- 5 methodology critical warnings;
- post-change RTL simulation for realtime ERROR crossing;
- post-change implementation timing;
- bitstream generation and board loading;
- real scan-to-lock transition;
- real P-only lock.

## 10. Gate decision

The project may leave the old `ramp timing repair` gate and enter:

```text
LOCK-MVP-L1 / FPGA Realtime ERROR-Crossing Acquisition
```

The production bitstream remains blocked until:

1. the new realtime crossing RTL regression passes;
2. the unconstrained/no-clock reports are classified;
3. the modified RTL is re-synthesized and re-implemented;
4. the new routed result still has non-negative setup and hold slack;
5. no new acquisition path becomes timing critical.
